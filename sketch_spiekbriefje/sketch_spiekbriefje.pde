void setup(){
 size(525,620); 
}

void draw(){
 background(255,255,255); 
 
 fill(0);
 textSize(17);
 text("All the codes:",5,20);
 text("println, print, fill, noFill, size, background, text, line",5,60);
 text("stroke, strokeWeight, rect, ellipse, triangle, (teken codes)",5,80);
 
 text("int NaamVanVariabel, float, string, if statement, else", 5,120);
 text("statement, else if statement, &&, ||, <, >, == zijn operators", 5, 140);
 text("boolean, dit zijn de variabels", 5, 160);
 
 text("switch case, case, break, default, dit zijn de switch cases",5, 180);
 
 text("for loop, while loop, in for loop heb je variabel, de waarde, ++i",5,220);
 text("dit waren de for en while loops",5,240);
 
 text("void setup, void draw, noLoop, loop, redraw, < de methodes en functie", 5, 260);
 
 text("cp.addButton, .setPosition, .setSize, .setCaptionLabel, setColorActive", 5, 300);
 text("setColorForeground, setColorLabel, setColorBackground, hide, show", 5, 320);
 text("cp.getController, Interger.valueOf, dit zijn de imports, knoppen,tekstveld", 5, 340);
 
 text("int[] variabelNaam, dit is een array", 5, 380);
 
 text("keyPressed, keyReleased, mousePressed, mouseReleased, mouseClicked",5, 420);
 text("mouseMoved, mouseDragged, mouseX, mouseY, dit zijn de inputs", 5, 440);
 
 
 
 
 
 
 
 
 
 
 
 
 
}
